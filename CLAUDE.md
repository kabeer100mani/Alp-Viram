# Alp-Viram

> Persistent project context for Claude Code. Keep this file up to date as milestones complete or decisions are made (standing instruction from Palash — no need to be asked each time). Last updated: 2026-07-15.

## Project Overview
- Alp-Viram is an **AI-first work management platform** built around **minimizing user effort above all else**.
- The **AI Inbox** is the primary differentiating feature.
- The **design package is locked and frozen as of 2026-07-11**. No changes to it without explicit sign-off.

## Tech Stack
- **Vite** (frontend build)
- **Supabase** (backend / DB / auth / Edge Functions)
- **shadcn/ui** (component library)
- **TanStack Query** (server state)
- **Zustand** (client state)
- **Zod** (schema validation)

## Data Model
- Three stored item types: **Task, Note, Meeting**.
- **Hybrid responsibility model** for permissions/ownership.
- **Provider-agnostic AI contract**, validated via **Zod before any AI output is trusted** — no AI response bypasses validation.

## Architectural Decisions
_(Already made — do not revisit without discussion.)_
- **Hosted Supabase** chosen over local Docker.
- **Edge Functions** are the primary path for privileged actions; **RLS is defense-in-depth**, not the sole control.
- The **`activity_events` table is append-only** (intent: PDL-018 / TDL-004). ⚠️ *Accuracy note:* this was **not** enforced from the first migration — `0002` allowed client INSERTs and `0003` removed that policy. It is enforced **against clients** today, but **not against the service role** (see TD-002), so "append-only" is not yet fully true.
- **AI provider — currently ACTIVE: Gemini** (`AI_PROVIDER=gemini`, `GEMINI_MODEL=gemini-flash-lite-latest`), chosen **on cost grounds** (free tier) while Anthropic is unfunded. **Anthropic/Claude remains the intended long-term provider**; its code path is implemented but unvalidated pending credits. Swapping is an `AI_PROVIDER` secret change + redeploy — no app changes (proven across three providers in M3).

## Milestone Status
> **Numbering corrected 2026-07-16 (Palash).** The AI Inbox was previously labelled "M3"; it is the plan's **M4 (AI layer)** — `docs/PROJECT_PLAN.md` §7 and its Revised milestone note both say M4, as do `src/lib/ai/types.ts` and `provider.ts`. The plan's real **M3 (Workspace UI) was skipped** and is next. The Permission Model is **not** in the plan's roadmap (permissions were folded into M1); it is recorded as an unnumbered milestone.

- **M0** — foundation (tooling, config, structure, themed app shell): **COMPLETE**
- **M1** — identity, multi-tenant foundation, RLS + isolation tests: **COMPLETE**
- **M2** — core domain schema (items, roles, assignments, activity_events, meeting_details): **COMPLETE**
- **M3 — Workspace UI**: **COMPLETE** — Gate A approved 2026-07-16, **Gate B approved** ("Gate B approved. Before M5: …"). See [M3 spec](docs/M3-workspace-ui-spec.md).
  - **Gate A** (`3358b02`): view engine (Zod-validated saved-view filters, TDL-010) · write path (none existed before) · intent-based rail · 7 system views seeded (`0007`) · one-action item cards · `writable_item_ids` RPC (`0008`).
  - **Gate B** (`fd45618`): **Daily Review** (rollover → grouped triage → "Inbox clear", 5–10 min target) · **Search** (full-text over the `items.search` tsvector) · theme persistence verified.
  - **No board view** (**PDL-029**) — `PROJECT_PLAN.md`'s "board views" line predated the 2026-07-11 freeze and contradicted Doc 5, which rejects boards by name; the roadmap line is corrected. **Daily Review + Search in M3** (**PDL-030**).
  - Fixed three shipped violations of the frozen package: `HomeScreen` showed org/role to solo users (PDL-022), `ItemList` printed the raw `item_state` enum (PDL-027), `AppShell` had no rail.
  - Verified: **browser walkthrough 32/32** (`node scripts/m3-walkthrough.mjs`, needs `npm run dev`) · live check that the UI never offers an action RLS refuses. ⚠️ This walkthrough had been **stale since PDL-034** — it drove the deleted `ItemCard`'s Done button and failed on step 5; it was not re-run when the card became a table. Now updated to the row's Status control. **Re-run the browser walkthroughs whenever a surface is replaced** — unit tests mock the DB and caught none of this.
  - ⚠️ The "≤3 interactions / ≤10s capture" figures in Doc 6 are **not a spec** — PDL-026 defers targets until after the prototype. The only frozen number is **Daily Review 5–10 min**.
  - Not built (Good-to-Have, "only if cheap"): duplicate detection/merge in triage, AI auto-grouping beyond type, drag-to-calendar timeboxing. *(The old note here — "Projects and People & Roles rail surfaces remain unbuilt (Projects sits disabled)" — is **out of date**: the Projects tree shipped in PDL-035 (`ListTreeNav`) and People & Roles shipped in M5.)*
- **M4 — AI Inbox / AI layer**: **COMPLETE — validated on Gemini `flash-lite`** (accepted at gate by Palash 2026-07-15). *(Previously mislabelled M3.)*
- **M5 — People & Roles + Responsibility**: **COMPLETE — signed off by Palash 2026-07-16** (its known gaps stay open, not carried as done: org rename + switcher moved to M6 Gate A). Spec [docs/M5-people-roles-spec.md](docs/M5-people-roles-spec.md); report [docs/M5-milestone-report.md](docs/M5-milestone-report.md). Chosen over the roadmap's "assistant depth" (Future scope) — PDL-031. This makes the responsibility differentiator (enforced since the Permission Model milestone) finally **reachable** in the UI.
  - **Gate A** (`dd07de1`): link/token invites (migration `0009` + `invitations` Edge Function; email deferred, PDL-011), Members list, `/invite` accept, persisted **active-org** primitive ([active-org-store.ts](src/modules/organizations/active-org-store.ts); seed of the PDL-009 switcher).
  - **Gate B** (`2f4fa40`): roles (create/rename/retire) + time-bounded assignments + By Role grouping.
  - **Gate C** (`0410aa0`): responsibility on the item card (Responsible Roles / Assigned Users / Collaborators; "UNFILLED — needs owner"; derived holder), live triage Role chip, solo-invite escape hatch (closes the Gate A deadlock), role rename.
  - Verified live: `m5-invite-test` 12/12, `m5-roles-test` 12/12 (incl. **handover moves responsibility with zero item-row changes**), `m5-responsibility-test` 7/7 (responsibility follows `can_write_item`), `m5-invite-walkthrough` 20/20 (two-user browser), 57 unit tests.
  - **Edge Functions now**: `classify-capture`, `invitations` (both `verify_jwt=true`; `npm run deploy:function` deploys both + JWT guard).
  - **Known gaps**: org rename unbuilt (team sees admin's personal-org name); no member-removal/role-change UI; point-in-time responsibility read model deferred; delegations (TD-004) not built.
- **M6 — MVP completion**: **COMPLETE — both gates signed off by Palash (Gate A 2026-07-16, Gate B 2026-07-17).** Renumbered from the roadmap's "Collaboration" (**PDL-038**; the third pre-freeze roadmap correction after PDL-029/PDL-031). Covers the five unbuilt Doc 4 Must Haves — **Tags · org rename · multi-org switcher (PDL-009) · custom saved views · reminder surfacing (PDL-011)** — plus a **TD-006** tag-along. See [M6 spec](docs/M6-mvp-completion-spec.md).
  - **Gate B — reminder surfacing (the real bug, PDL-011):** `items.remind_at` was written by the AI Inbox but **read by nothing** — the `due` window resolved `due_at` only, so a reminder-only item ("remind me tomorrow", no due date) surfaced in **no view** once triaged. Fixed by migration **`0017`**: a `nudge_at` **generated** column = `least(due_at, remind_at)` (`least` ignores NULLs = "first moment this needs a human"; **not** `coalesce`, which would let a later due date hide an earlier reminder), indexed. Today/Upcoming + the `due_asc` sort now resolve against `nudge_at`. `remind_at` is now **editable** (a `datetime-local` "Remind me" quick field in the TaskPanel) with a **bell indicator** on the row. **TD-005 timezone discipline held**: verified live in **IST** — a 09:00 IST reminder stores as **03:30 UTC**, not 09:00 UTC.
  - **Gate B — custom saved views (Doc 4 "system + custom"):** create/rename/delete from the rail (`ViewEditor` form over the **existing** `viewFilterSchema` — no new filter dimension; the repo **re-validates with Zod before storing**, TDL-010). **Owner-private (D4)** and **system views read-only** — both already enforced by `saved_views` RLS (`p_views_read`: `is_system OR owner_id=auth.uid()`; `p_views_write` blocks `is_system=true`); the repo mirrors it with `.eq('is_system', false)` guards + silent-denial→`PermissionError`.
  - Verified: **93 unit tests** (view-CRUD Zod-before-store + silent-denial; the agent-written suite reviewed); `scripts/m10-reminder-test.mjs` **8/8 live in IST** (the bug fixed + the TD-005 round trip); `scripts/m10-mvp-walkthrough.mjs` **18/18 browser** (now incl. view create/rename/delete, owner-private, system views have no edit control). m3/m7/m9 still green.
  - **Gate A** — **Tags**: `src/modules/tags/` (repository/hooks/`TagChip`/`TagPicker`); create-inline while tagging (no setup gate, PDL-005); tag chips on the row (click filters via a new `tag` pane), tag editing in the TaskPanel; **tags apply to Notes too** (checklist/DoD do not). `tags` added to `viewFilterSchema` (ids not names, so rename can't break a saved view) + a `runView` join path. **No migration** — schema is from `0002` with the `0003` composite FKs; **`p_item_tags_write` requires `can_write_item`**, so tagging follows the item's write rules and the UI hides the control when `!canWrite`.
  - **Gate A** — **Org rename** (`renameOrganization`, admin-only mirroring `orgs_update`; **slug left stable**, D2) + **multi-org switcher** (`OrgBar`; `useMyOrgs`/`useSwitchOrg`). Switching calls `queryClient.clear()` — a stale cache would *show* the previous tenant's rows even though RLS would never re-serve them, which looks identical to a leak. **PDL-022 preserved:** `OrgBar` renders nothing for a solo user with one org.
  - **Gate A** — **TD-006 RESOLVED**: the "confidence 100%" chip is removed from the AI proposal (it was 1.0 on 28/30 incl. the miss); `confidence` is **still stored** on `ai_captures`. Register updated.
  - **New debt — TD-010** (D1): tag delete is hard + cascades org-wide; **restricted to admins in the UI only — NOT a security boundary** (RLS `p_tags` still lets any member delete via API). Soft-delete deferred.
  - Verified: **81 unit tests** (incl. tag/org silent-denial + `viewFilterSchema` tags); `scripts/m10-tags-test.mjs` **12/12 live red-team** (member can only tag what they can write; no cross-tenant tag/rename; positive control; slug stable); `scripts/m10-mvp-walkthrough.mjs` **12/12 browser** (tag→filter, switcher, **no cross-org bleed after switch**, no confidence chip). m3/m7/m9 still green.
  - **Four of the five need NO migration** — `tags`/`item_tags` (composite FKs from `0003`), `orgs_update` (`is_org_admin`), `saved_views` (owner-private) are already built and hardened; the gap is UI. `useActiveOrg` already fetches every org and discards all but one, so the switcher is nearly free.
  - **The real bug (Gate B):** `items.remind_at` is **queried nowhere** — `views-repository` resolves the `due` window against `due_at` only. An AI-captured reminder (`remind_at` set, `due_at` null) appears in **no view** once triaged out of the Inbox, though PDL-011 requires reminders to work in Today/Due. Proposed fix: a `nudge_at = least(due_at, remind_at)` generated column (`least()` ignores NULLs = "first moment this needs a human"; `coalesce` would let a Friday due date hide a Tuesday reminder).
- **Permission Model** (unnumbered — not in the plan's roadmap): **COMPLETE**, accepted at gate 2026-07-15. Migration `0005_permission_hardening.sql` (applied): access control keyed to the hybrid responsibility model. Closes **TD-001**, **TD-002**, and implements **TDL-012** (the derivation — previously the log's only *Proposed* decision).
  - Write = org admin **or** creator (PDL-021) **or** assigned user **or** current holder of a responsible role (derived, time-bounded). Collaborators get no write. **Read deliberately unchanged** (org-wide; fine-grained visibility stays deferred).
  - Verified: `node scripts/m5-permission-test.mjs` — **22 passing + 3 known failures (TD-007)**; incl. time-bounded revocation, self-assignment escalation, and the service role being unable to rewrite the audit log. `m1-isolation-test.mjs` still 14/14. Migration `0006` additionally fixed a pre-existing bug (org delete blocked by its own owner-protection trigger).
  - **DB access note:** `db.<ref>.supabase.co` is **IPv6-only** and unreachable from this environment (TD-003). Use the IPv4 session pooler — `PGHOST`/`PGPORT`/`PGUSER` in `.env` are set to it; apply migrations with `node scripts/db-apply.mjs <file>`.
  - Documented **after** implementation: [Doc 8 — Permission Model](docs/technical/08-permission-model.md), [Doc 9 — Responsibility Model](docs/technical/09-responsibility-model.md).
### M4 — AI Inbox (detail)
Pipeline and provider-agnosticism proven; **explicitly NOT Anthropic-verified**. See [M4 milestone report](docs/M4-milestone-report.md).
  - Accepted run, `gemini-flash-lite-latest`: 30/30 classified, **30/30 Zod-valid**, 30/30 stored + read back, **29/30 correct type (97%)**. Zero task-vs-meeting disagreements. The one miss — "Idea: add dark mode to the app" → task (expected note) — is **adjudicated as a genuine miss**, not a defensible call.
  - Partial run, `gemini-flash-latest` (fuller Flash): 16/30 — 16/16 correct, 16/16 Zod-valid — truncated by the free-tier **daily** request quota.
  - **Anthropic re-test still PENDING FUNDING.** The Anthropic code path has never had a successful real run (account has no credits). Re-test when funded — M4 is not evidence about Claude's accuracy.
  - Providers supported in the Edge Function: `anthropic` | `gemini` | `mock`, via the `AI_PROVIDER` secret. `verify_jwt = true` is pinned in `supabase/config.toml`.

## Structure reversal (PDL-032/033, 2026-07-16)
- **PDL-006 was reversed** at Palash's direction (recorded, not silent): `Organization → Folder → List → Item` is back, **optional** — "Workspace" = the existing Organization (Personal / Profile 1…). Cheapest path: `projects` renamed to `lists`, `folders` added above. `list_id`/`folder_id` are nullable — **capture stays zero-click**; the AI does **not** infer the list. Amended docs: PDL-006 (superseded), PDL-010, PRD §problem-statement + "Flat over deep", Doc 4 Rejected row, Doc 5 model.
- **Checklists + Definition of Done** added (PDL-033). DoD is a **note field, not enforced**. Neither applies to Notes.
- **TD-008 fixed** (was a real cross-tenant FK gap on `items.project_id`; migration `0011` composite FK). **Audit-trigger regression** from the rename fixed in `0013` (`log_item_change` still named `project_id`, breaking every item write — caught by the live red-team, invisible to unit tests).
- Migrations `0010`–`0013`. Tests: `scripts/m6-structure-test.mjs` (15/15 live tenant-safety), `scripts/m6-structure-walkthrough.mjs` (12/12 browser). See [impact report](docs/impact-hierarchy-reversal.md).

## Container hierarchy — Project → Folder → List (PDL-035, 2026-07-16)
- **Full ClickUp-style nesting:** `Organization (= Workspace) → Project → Folder → List → Item`. **Corrects PDL-032**, which had collapsed Project into List on my (wrong) recommendation. Migration `0015` adds `projects` (org-level), re-parents Folder (`folders.project_id`) and List (`lists.project_id`); a List may be folderless (directly under a Project). Subtasks skipped.
- **Everything optional** — capture stays **zero-click** (a task needs no List → Inbox; a List needs no Folder). Filing happens in Daily Review or manually, never forced.
- **List ≠ Checklist:** a List holds Tasks; a Checklist is sub-steps inside one Task. The row-expand keeps them in separate labelled sections, and the DoD is shown directly (no longer buried).
- Rail is a 3-level tree (`ListTreeNav`): Projects/Folders expand; only **Lists** are selectable (tasks live in Lists). Verified: `scripts/m8-hierarchy-test.mjs` (10/10 live: tenant + cross-project integrity, folderless, optional), `scripts/m8-hierarchy-walkthrough.mjs` (6/6 browser).

## Dense table layout (PDL-034, 2026-07-16)
- **The item card is replaced by a dense, table-style layout** (`ItemTable` + `ItemRow`), used by **every** view. ⚠️ **Amended by PDL-036 below:** columns are now Name · Assignee (avatar-only, team-only, PDL-022) · Priority · Due date · Status — **Start moved off the row into the panel**, and the **row-expand no longer exists**. **Priority/Due/Status still edit inline in the row.** Items render in **collapsible groups with counts** (By Role → by role; every other view → by List). Old `ItemCard`/`ItemList` **deleted**.
- `start_at` promoted from *Future* to a real nullable column (migration `0014`); it was the one requested column with no existing field. Batched **`item_assignee_summary`** RPC feeds the Assignee column in one call (not per-row). Plan: [docs/plan-dense-table-view.md](docs/plan-dense-table-view.md).
- Verified: `scripts/m7-table-walkthrough.mjs` (13/13 browser: grouping, inline edits persist, row-expand, solo hides Assignee), 56 unit tests.

## M7 — Trust & Lifecycle — COMPLETE (signed off by Palash 2026-07-17, both gates)
Tier-1 MVP blockers from [the gap analysis](docs/MVP-gap-analysis.md). Renumbered from the roadmap's "Files & notifications" (**PDL-040** — Future/Good-to-Have, same pre-freeze trap as PDL-038). Spec: [docs/M7-trust-lifecycle-spec.md](docs/M7-trust-lifecycle-spec.md). All four Tier-1 blockers closed: password reset, member offboarding, org deletion (TD-007), snooze wake (TD-011).
- **Invite email — deliberately deferred (Palash, 2026-07-17), not a pending task.** No domain for now; the current path is the **shareable-link invite fallback** + **Supabase's built-in password-reset sender** (both work today). The Resend send-path is already coded and gated behind `RESEND_API_KEY` (dormant). Revisit real invite emails **closer to real launch** — then buy a domain, set `RESEND_API_KEY`/`RESEND_FROM`, and redeploy the `invitations` function. Consistent with PDL-011 (email delivery deferred).
- **Parked, waiting on Palash (not on code):** **Anthropic accuracy batch (D4)** — when funded, set `AI_PROVIDER=anthropic`, `npm run deploy:function`, run `node scripts/m3-classify-test.mjs`, and fix **TD-012** (provider mislabel) at the same time.
- **On hold (Palash, 2026-07-17):** do **not** start Tier-2 (CI, error boundary + read-error states, observability, tag soft-delete/TD-010) until he returns after the domain/Resend + Anthropic funding are sorted — then decide next scope together.
- **Gate B — org deletion (TD-007 ✅ RESOLVED):** migration `0018` lets teardown complete (audit triggers skip logging on DELETE when the org is gone; the append-only guard allows the `item_id` SET NULL UPDATE when the org is gone — same "parent already deleted ⇒ teardown" test as 0005/0006). Migration `0019` adds an **owner-only `orgs_delete`** policy so it's reachable. Minimal **type-to-confirm "delete workspace"** UI in the People screen (owner-only). **Live-org append-only invariant intact** — `m5-permission-test` now all-green (the 3 known-failures pass) with the service-role positive control still refusing; `m1-isolation` 14/14.
- **Gate B — snooze wake (TD-011 ✅ RESOLVED):** migration `0020` adds **`wake_due_snoozes()`** (org-scoped, timezone-safe) called on load + Daily Review open (`useWakeDueSnoozes`); a due snooze returns to `committed`. Seeds a **"Snoozed" system view** (8th) so deferred items stay findable; **`snoozed` dropped from the hand-set status dropdown** (D-c) — snooze is a defer-*until* action only. Verified live in **IST** (`m11-snooze-test`): due snooze wakes, future snooze stays hidden.
- Gate B verification: `scripts/m11-snooze-test.mjs` **9/9** (IST), `scripts/m11-offboarding-test.mjs` extended with org-delete (owner-only + cascade), lifecycle walkthrough +danger-zone confirm gate; 93 unit tests; m3/m10/m11 green.
### M7 Gate A (accounts & team, 2026-07-17)
- **Meeting dropped (PDL-039):** capture/triage offer **Task/Note only**; `classify.ts` **coerces a `meeting` classification → `task`** (belt-and-suspenders — the enum + `meeting_details` stay dormant in the schema, revivable, no migration). Existing `meeting`-typed rows still render.
- **Member offboarding:** `PeopleScreen` gains admin controls — **change role · deactivate/reactivate · remove** (two-step confirm). **No new RLS/migration** — `members_update`/`members_delete` already require `is_org_admin`, and `protect_owner_membership` (0003) refuses removing/demoting/deactivating the **last active owner** and non-owner owner-grants. Actions on your **own** row are withheld (no self-lockout). Admins see deactivated members (via `listMembers(orgId, {includeInactive})`); the responsibility pickers still get **active-only**.
  - ⚠️ **TD-013:** a deactivated member shows as "Member (Deactivated)", not their name — `profiles_select`/`shares_org_with` require *both* memberships active. Minor, arguably-correct, fix needs an RLS-visibility decision.
- **Password reset:** "Forgot password?" → `resetPasswordForEmail` (generic "if an account exists" copy — no enumeration) → **public `/reset-password` route** that sets the new password in the recovery session. Reuses the shared password validation.
- **Invite email (D1, Resend):** the `invitations` Edge Function sends the invite link via Resend **when `RESEND_API_KEY` is set** — otherwise it **gracefully falls back to the shareable link** (never blocks the invite). Client passes `appUrl`; server validates it and returns `emailed`.
- **⚠️ Deploy/config still needed for email to actually send:** (1) **redeploy** the `invitations` function (`npm run deploy:function`) so the email code is live; (2) set the **`RESEND_API_KEY`** (+ optional `RESEND_FROM`) Supabase secret once Palash's Resend account + **verified sending domain** exist (domain being purchased — sandbox/`onboarding@resend.dev` meanwhile). Password-reset emails send via Supabase's built-in sender already (rate-limited); custom SMTP improves deliverability.
- Verified: `scripts/m11-offboarding-test.mjs` **13/13 live red-team** (admin-only; last-owner guard; owner-grant guard; positive control); `scripts/m11-lifecycle-walkthrough.mjs` **13/13 browser** (Meeting gone, deactivate/reactivate/remove, no self-lockout, password reset request + reset page + sign-in with the new password); 93 unit tests; m3/m5-invite/m10 still green.

## Task detail side-panel (PDL-036, 2026-07-16)
- **The row-expand is replaced by a slide-in task detail panel** (`TaskPanel`), opened by clicking a task's **name**. It does **not** navigate away — the list stays mounted; Escape, the close button and the scrim all dismiss it. Contents: inline-editable title · quick fields (Status + complete checkmark, Dates start→due, Priority, **Time estimate**, List) · description (mapped to the existing `items.body`) · a **Fields** section (Checklist + Definition of Done, both shown directly) · a read-only **Activity** feed.
- **UI only** — every write goes through the same hooks the row used, so `writable_item_ids`/RLS and Zod validation are unchanged, and no editor is offered that the DB would refuse.
- `time_estimate_minutes` promoted from *Future* to a real nullable column (migration `0016`, `>0` + not-on-a-Note checks). It is an **estimate** (planned duration, entered "2h 30m") — **time tracking is still rejected**.
- **Activity is read-only; comment-writing deferred.** It renders `activity_events` through `activityLabel()` (never the raw enum, PDL-027). ⚠️ **Field edits are not logged at all** (no such event type exists) → **TD-009**; the feed shows creation/state/completion/list-moves/responsibility/tags only.
- Free-text fields (title, description, estimate) save on **blur** and are deliberately **not** disabled while other saves are in flight — disabling mid-typing silently dropped keystrokes (caught by the walkthrough).
- Verified: `scripts/m9-panel-walkthrough.mjs` (**27/27** browser, incl. every quick field persisting to the DB and a **geometry check** that no field is clipped — the first cut rendered Due half outside the panel while every value assertion still passed), 61 unit tests.

## Dates and enums must never reach the user raw (2026-07-16)
Three bugs Palash caught by *looking* at the screen, all invisible to green tests:
- **A bare `<input type="date">` prints the browser's own "dd-mm-yyyy" placeholder** as literal text when empty (clipped to "dd-mm-yy" in a narrow cell), and a raw "2026-08-20" when set. Use **[DateCell](src/modules/items/components/DateCell.tsx)** — it shows `formatDate()` (or a quiet hint) and swaps in the input only while editing. Native date inputs are kept **only** in the TaskPanel form. The same bug was live on the **Daily Review triage chip**, where empty is the *common* case.
- **Raw ISO / raw enums must go through [presentation.ts](src/modules/items/presentation.ts)** (`formatDate`, `priorityLabel`, `itemStateLabel`, `itemTypeLabel`, `activityLabel`) — PDL-027. The AI proposal card printed `Due 2026-07-16` on the primary capture path, and a **unit test asserted that ISO string**, pinning the bug.
- **`priority: none` labelled `—`** rendered as a stray dash between the flag and the dropdown arrow; display labels are not prescribed by the frozen docs (only the enum values are), so it now reads "None". The select's native arrow is gone; the cell shows one clean flag + label.
- **Still open (found by audit, awaiting Palash's styling direction):** bare `—` for empty assignee ([ItemRow.tsx](src/modules/items/components/ItemRow.tsx)) and for read-only dates/estimate ([TaskPanel.tsx](src/modules/items/components/TaskPanel.tsx)); `{m.role}` rendered raw in [PeopleScreen.tsx](src/modules/people/components/PeopleScreen.tsx) — masked by `capitalize` today, but there is **no `roleLabel()` helper**, so it has no label path.

## PWA + deployment (PDL-041, 2026-07-17)
- **Installable PWA** — `public/manifest.webmanifest`, icons in `public/icons/` (generated from `public/favicon.svg` by `npm run gen:icons` via Playwright — no image dep; re-run if the mark changes), a **conservative** service worker `public/sw.js` (network-first; **never intercepts cross-origin/Supabase**; cache only as offline fallback — dodges the stale-PWA-after-deploy trap), registered **prod-only** in `main.tsx`. `index.html` carries the manifest link, `apple-touch-icon`, and theme-color. Pulled forward from Doc 4 **Future** scope (additive, low-risk); a native/Play-Store build is NOT included.
- **Deployment: Vercel (frontend) + Supabase (backend, unchanged).** `vercel.json` gives SPA rewrites (BrowserRouter deep links like `/reset-password` must not 404) + a no-cache header on `/sw.js`. Founder-run guide: **[docs/DEPLOY.md](docs/DEPLOY.md)**. Two build-time env vars: `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY` (both public/browser keys). **After deploy, the Vercel URL must be added to Supabase → Auth → URL Configuration (Site URL + Redirect URLs)** or password-reset links break.
- ⚠️ **The repo's ~37 latest commits (M3→PWA) are committed locally but NOT pushed to GitHub** (branch `Development`, 37 ahead of origin). Vercel deploys from GitHub, so a push is required first. Branches are `Development`/`Test`/`Production` (no `main`) — the Vercel "production branch" must be pointed at one of these. **Pushing/merging awaits Palash's explicit go** (push-only-when-asked).
- Verified: `npm run build` clean; `scripts/pwa-verify.mjs` **14/14** against the built app on `npm run preview` (manifest valid, all icons 200, standalone display, SW registers, cross-origin left to network).

## UI Style Rules
- **The `captured` state displays as "To Do", not "Inbox"** (PDL-037, ruled by Palash 2026-07-16). "Inbox" now names **only the view** in the rail (and "Inbox (no list)" as an item's *location*). The `captured` enum value is unchanged. This **amends the frozen package** — Doc 5 `:63`/`:196` and the ERD `:135` all said "Inbox"; all three are annotated. Doc 5 §2a's modelling argument ("Inbox is a state, not a container") **still stands** — only the label changed, because the doc had reused the view's word as the state's name.
- **No ALL-CAPS text anywhere.** Everything is **Proper Case**; the only exception is the app name ("Alp-Viram"). Never use the Tailwind `uppercase` class. DB-lowercase values (e.g. member `role`) render with `capitalize`. *(Palash, 2026-07-16.)*

## Deploy Safety Notes
- **Never deploy Edge Functions with `--no-verify-jwt`** without an explicit, logged exception. On 2026-07-15 that flag was used by mistake and briefly left `classify-capture` open to unauthenticated requests; fixed by pinning `verify_jwt = true` in `supabase/config.toml`.
- **Deploy via `npm run deploy:function`** — the JWT guard runs automatically, chained to the deploy (`deploy && npm run check:jwt-guard`). The guard ([scripts/check-jwt-guard.mjs](scripts/check-jwt-guard.mjs)) probes `classify-capture` unauthenticated; if it doesn't get a **401** the chained command fails loudly with a non-zero exit (failure is not swallowed).

## Technical Debt Register
**Canonical register: [docs/technical/TECHNICAL_DEBT.md](docs/technical/TECHNICAL_DEBT.md)** — always add new debt there (never invent IDs here; they collided once already). Summary only:
- **TD-001** — ✅ **RESOLVED 2026-07-15** (migration `0005`): hard-delete revoked on `items`/`roles`/`role_assignments`; retire via `deleted_at`/`is_active`/`valid_to`.
- **TD-002** — ✅ **RESOLVED 2026-07-15** (migration `0005`): `activity_events` guarded by a `BEFORE UPDATE/DELETE` trigger. **Verified against the real service role** (`sb_secret_` key, with a positive control proving it bypasses RLS): it cannot UPDATE or DELETE audit rows.
- **TD-007** (open, 2026-07-15): **organization deletion is incomplete.** `0006` fixed the last-owner block; the `0002` audit triggers (FK violation) and the `item_id SET NULL` cascade still block teardown. No security impact; blocks offboarding/GDPR erasure. Fix drafted in the register. `m5-permission-test.mjs` marks 3 checks as **known failures**.
- **TD-003** (open): Supabase client is untyped (`getSupabaseClient()` not parameterised with `<Database>`).
- **TD-004** (open): deferred tables (`recurrence_rules`, `attachments`, `delegations`) need RLS + composite FKs when they ship.
- **TD-005** — ✅ **RESOLVED 2026-07-15**: AI naive/timezone-less datetimes caused a real time-shift risk (a 4pm IST reminder would have fired at 21:30 IST). Fixed: client sends IANA timezone, Edge Function resolves in the user's zone + normalises, Zod now requires `z.iso.datetime({ offset: true })`. Verified 4/4 in IST; 6 unit tests pin it.
- **TD-006** — ✅ **RESOLVED 2026-07-16** (M6 Gate A): the degenerate `confidence` (1.0 on 28/30 incl. the miss) is **no longer displayed**; still stored on `ai_captures` for future calibration.
- **TD-010** (open, 2026-07-16): tag **hard-delete cascades org-wide**; admin-gated in the UI only — **not** a security boundary (RLS still permits any member to delete via API). Soft-delete deferred (M6 D1).
- **TD-009** (open, 2026-07-16): **field edits are not audited** — no `activity_event_type` covers a title/priority/date/description change, so the PDL-036 Activity feed cannot show them. No security impact; fix when the audit schema is next opened.

## Tools & Resources
- **Supabase project ref**: `jdngjwspqxhpkmqhcekc`
- **Supabase URL**: https://jdngjwspqxhpkmqhcekc.supabase.co
- **IDE**: VS Code with the Claude Code extension.

## Working Relationship Notes
- **Palash** is a **non-technical founder acting as Product Architect**.
- Reviews output at **milestone gates** before approving progression.
- **Always explain terminal/dashboard actions in plain language** with exact step-by-step commands.
- **Provide ready-to-use prompts** — don't assume Palash will write his own.

## Long-term vision (not buildable — see [docs/FUTURE-VISION.md](docs/FUTURE-VISION.md))
Palash's ultimate goal (2026-07-18): the system eventually **monitors real work across a user's tools and auto-infers done-vs-pending**, ending manual status updates — the status-effort analogue of today's capture-effort minimization. Explicitly **future**, needs integrations + reliable inference + privacy design + a trusted AI foundation (PDL-031). Recorded so it isn't lost; **not scoped, not on any milestone.** The append-only activity log + responsibility model are its honest groundwork.

## Next Steps On The Horizon
> **CURRENT (2026-07-17): the MVP gap analysis is written — [docs/MVP-gap-analysis.md](docs/MVP-gap-analysis.md) — awaiting Palash's scoping decision.** All Doc 4 feature Must-Haves are built; what's left is lifecycle/operability breadth. **Tier 1 blockers:** no password reset · no member offboarding (removal/deactivate/role-change) · org deletion still fails (TD-007) · **snooze never wakes (TD-011, found in the audit)**. **Tier 2:** tag hard-delete (TD-010) · no CI · read-failures show as empty states + no error boundary · AI accuracy is a single 30-item Gemini run & Claude never ran · no production observability. **Tier 3 (thin, not missing):** Meeting is Task-with-a-label (`meeting_details` never written) · no container rename/archive UI · field edits unaudited (TD-009) · no magic-link · `ai_captures.provider` hardcoded 'anthropic' (TD-012). Proposed shape: one milestone, Gate A "Trust & lifecycle" + Gate B "Operability & safety". **Four decisions open (D1 email delivery, D2 scope, D3 Meeting, D4 AI accuracy).** Do NOT start M7 as written (Future/Good-to-Have scope, PDL-038 note).
> Rewritten 2026-07-16. The old text here was **stale and actively misleading**: it named the Permission Model as *current* with "no written spec" and claimed Docs 8/9 "do not exist on disk". That milestone is **COMPLETE** (accepted 2026-07-15) and both docs **exist**. A session resuming from that text would have rebuilt finished work.

1. **The plan's remaining roadmap (M6 Collaboration, M7 Files & notifications) largely contradicts the frozen package** — the same trap as PDL-029 (board views) and PDL-031 (assistant depth): §8's roadmap **predates the 2026-07-11 freeze**. Doc 4 files **Comments** and **file attachments** as *Good-to-Have* ("MVP only if cheap"), **Real-time collaboration/presence** as *Future*, and **Notification delivery** as *Future*; PRD `:143` puts "deep real-time collaboration" and "notification delivery channels" in **explicitly NOT in the MVP**, and PRD §7 lists "Deeper collaboration — comments, mentions" as Future. M6's "assignments" already shipped in **M5**. **Do not start M6 as written without a PDL.**
2. **What MVP actually still needs — five Doc 4 *Must Haves* remain unbuilt** (verified on disk 2026-07-16, not inferred):
   - **Tags (flat)** — `tags` + `item_tags` have existed since migration `0002`; there is **no tags module and zero UI**.
   - **Organization renamable** — Must Have reads "Organization (auto-created, **renamable**, invite members)". Only *role* rename exists (`useRenameRole`); a team still sees the admin's personal-org name.
   - **Multi-org switcher (PDL-009)** — only the `active-org-store` primitive exists; no switcher UI.
   - **Custom saved views** — Must Have reads "system … **+ custom**". `src/modules/views/components/` holds **only** `ViewRail.tsx`; no create/edit-view UI.
   - **Reminders (in-app)** — `is_reminder`/`remind_at` exist in the DB and the AI contract, but **nothing surfaces a nudge**; the fields appear in no UI component.
3. **M5 has never been formally signed off** (Gates A–C built & verified). `PROJECT_PLAN.md` §"Milestone reporting": *"No next milestone starts until the current one is completed, tested, and you approve."*
4. **When Anthropic is funded**: set `AI_PROVIDER=anthropic`, `npm run deploy:function`, re-run `node scripts/m3-classify-test.mjs` for a Claude accuracy read (M4 says nothing about Claude's accuracy today).

## ⚠️ Script names are NOT plan milestones
`scripts/m6-*`, `m7-*`, `m8-*`, `m9-*` are named after the *work* they verify — structure reversal, dense table, hierarchy, task panel — **not** the roadmap's M6 (Collaboration) / M7 (Files & notifications) / M8 (Hardening). Likewise `m3-classify-test.mjs` tests **M4**'s AI layer. This is the same numbering confusion that already forced the M3/M4 correction — check what a script *does* before trusting its prefix.
