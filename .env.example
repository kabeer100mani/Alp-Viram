# Copy this file to `.env` and fill in real values. Never commit `.env`.

# ── Supabase (wired up in Milestone 1) ───────────────────────────────
# Create a project at https://supabase.com, then copy these from
# Project Settings → API.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
